# OD0-R50 Counterexamples and Refutations (append-only)

## REFUTED: naive record identity (full lineage, ell)
- The naive identity (full lineage, ell) is NOT invariant: suffix extensions of a lineage that exist only in the later-firing trajectory duplicate the same physical record (9 common events witnessed the failure before canonicalization; 0 after). The A10 write copies pi_ell, so all suffix extensions share one record. Recorded as a refuted naive identification.

## WITNESS: cumulative ledger quantities are quotient-dependent
- Per-step conservation (backlog balance, population balance, service split) holds for every kernel application under any quotient (frozen kernel identities, ledger.py conservation()). No cumulative horizon quantity beyond them is invariant: the exact witness pools the same two unit requests in one step vs two steps at the registered point (B=0,D=1,Gamma=1,P=0,H=0,m=0) and the total-served-forced distributions differ (certificates: cumulative_ledger_witness).

## WITNESS: quotients with coherence lifetime > 1 exist
- Lifetime = 1 for every object under BOTH members (verified exhaustively at k<=3 (T_sat) / k<=4 (T_dag)): the object z0 = {y, p} for p a parent of y has depth(z0) = depth(y)+1 and dag_size(z0) = dag_size(y)+1, so z0 fires at the very next step in both gradings and uses y at its full word depth. General theorem: for a quotient q, lifetime(y) = min over z with y in Par(z) of q(z) - q(y); lifetime == 1 for all y iff every object has a child in the immediately next step (both graded members satisfy via z0); quotients admitting lifetime > 1 are exactly those deferring EVERY child of some y past step q(y)+1 - ancestry-compatibility permits arbitrary deferral, so such quotients exist (witness: defer z0 of any y by one step).

## HOSTILE CONTROL HC1: selecting T_sat or T_dag by preference or saturation readout
- status: REJECTED
- obstruction/scope: No selection anywhere; the no-choice verdict is NOT_SEPARATED_BY_SOURCE; the saturation readout is identical in kind for both members and selects nothing.

## HOSTILE CONTROL HC2: using the saturation readout to justify a throttle premise in-round
- status: REJECTED
- obstruction/scope: Part 4 is a census recorded at Commit A before Part 3 results; RG1 is recorded, not selected, not tested dynamically.

## HOSTILE CONTROL HC3: singling out a Lambda_0 scan value as physical
- status: REJECTED
- obstruction/scope: All 1296 registered points reported identically; kappa is the max over the whole domain; no point is distinguished.

## HOSTILE CONTROL HC4: step = time; historical rounds = steps
- status: REJECTED
- obstruction/scope: Steps are quotient indices only; no duration or physical time appears; historical engine rounds nowhere identified with steps.

## HOSTILE CONTROL HC5: kappa or coherence lifetime used as a maturity threshold
- status: REJECTED
- obstruction/scope: kappa and lifetime are readouts; no epoch, regime label, or threshold is defined from them.

## HOSTILE CONTROL HC6: any historical numeric
- status: REJECTED
- obstruction/scope: All numbers are generated by this round's own exact computation or are frozen structural constants (11/13/22/26 request bounds, registered ranges) cited from frozen sources.

## HOSTILE CONTROL HC7: modification of frozen roots; BELL2 opened
- status: REJECTED
- obstruction/scope: Bytecode writing disabled before importing frozen CD0 source; worktree clean at start and end; BELL2 unopened.

## HOSTILE CONTROL HC8: any hand-produced hash
- status: REJECTED
- obstruction/scope: Every hash in R50 outputs is computed in-process (hashlib / git rev-parse) at recording time; the input lock records the rule as permanent.
