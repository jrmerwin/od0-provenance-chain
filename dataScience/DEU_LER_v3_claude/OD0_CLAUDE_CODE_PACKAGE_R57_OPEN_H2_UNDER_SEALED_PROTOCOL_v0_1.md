# OD0-R57 CLAUDE CODE PACKAGE v0.1

## Opening the H2 Collision-Age Holdout under the Sealed R56 Protocol — One Comparison, No Repair

### Position

R56 is frozen: commits `6659b9f` (A) / `1363e93` (B) / `0b5067b` (C-stamp), output manifest `275840d3…` (full hash in R56_PROVENANCE_STAMP.json). Verdict `OD0_R56_PASS_H2_PREREGISTERED_AND_M5_OPENED`. H2 preregistration sealed as one object, hash `298d21da…` (full hash in R56_H2_PREREGISTRATION.json): nine provenance-disclosed observables, prediction set P1–P6, comparison protocol with advance PASS/PARTIAL/FAIL rule and mandatory model-family caveat. Alphabet audit: `PAIRWISE_REDUCTION_CONVENTION`, exact label-emission statements scoped to Γ ≤ 3; Γ ≥ 4 readouts lower-bound-costed and flagged; theorems unaffected. P4 PROVEN (hard bound: ≤ Γ tokens served per step). Recurrence PROVEN (every object reused infinitely often a.s.; chains Θ(log N)). H2 sealed: Run3_Dijet PDF `16e9cfcd…`, 260,956 bytes.

R57 executes the sealed protocol and nothing else.

### Adjudication notes to carry

1. **Scope from the audit.** P4's exact label-emission claims hold at Γ ≤ 3. Readouts at Γ ≥ 4 are lower-bound-costed. The comparison uses theorem-grade predictions as load-bearing and labels every readout-grade item.
2. **Raw over summary.** If the repository referenced by the paper holds epoch-sweep outputs, candidate banks, or filter definitions, those are the primary H2 artifacts; the paper's summary numbers are annotations.
3. **Pre-committed FAIL diagnosis path.** If H2 reports decreasing availability of any structure family with age, that contradicts P1 (THEOREM: objects are never destroyed; CD0 adjunction is monotone). Under the sealed rule that is FAIL. The mandatory caveat then states whether the H2 substrate has a destruction mechanism (foam collapse, edge removal, dissolution). R58 diagnoses whether that is a foam-family artifact or a missing constructor primitive. Nothing softens the verdict.

---

# 0. Governing instruction

Open H2 only. Before any content is read: verify the PDF hash against the R55 pin and the preregistration hash against the R56 stamp. H3–H5 remain sealed (`parsed = false` at start and end). H1 is spent and is not consulted.

The sealed protocol (R56_H2_PREREGISTRATION.json, copied verbatim into R57_INPUT_LOCK.json with hash match):

1. **Compared:** reported H2 patterns invariant under monotone reparametrization of rounds — emergence orderings, availability monotonicity, freezing/saturation, dependence on capacity or family complexity, cross-substrate variability — against P1–P6 and the nine frozen observables, mapped **by definition** at opening. Names, words, counts are not maps.
2. **Rule:** PASS iff every mapped reparametrization-invariant pattern is consistent with the corresponding THEOREM-grade prediction and none contradicts one; PARTIAL if consistent but a stage-defining H2 observable is unmapped or matches only at READOUT grade; FAIL if a THEOREM-grade prediction is contradicted. Mismatches at equal prominence.
3. **Forbidden:** round-number alignment; rate comparison; any prediction added, criterion moved, observable renamed, or tower repaired in this round.
4. **Model-family caveat, mandatory:** the H2 engine's opportunity law and substrate, from source.

---

# 1. Locks (minimal)

Pin R56 stamp and the preregistration hash. Clean worktree at start and end. `BELL2_opened = false`. H3–H5 sentinels. Hash hygiene; stamp commit at the end.

# 2. Commits

Commit A (before any H2 content is read): this package verbatim; protocol copied with hash match; hash verification of the PDF; the **scope statement** (Γ ≤ 3 exact; Γ ≥ 4 readouts flagged) recorded. Commit B: repository pin, extraction, mapping, adjudication, outputs. Commit C: stamp.

---

# 3. Part 0 — Repository pin (at opening, before extraction)

Resolve the paper's repository reference to a commit hash from the reference text only; fetch or locate that commit; hash-pin its tree. Inventory (filenames, sizes, hashes, headings only) any epoch-sweep outputs, candidate banks, filter definitions, substrate archives, and the collision driver. Record which H2 claims have raw backing and which are paper-only. Record the H2 engine's opportunity law verbatim from its source for the caveat. If the repository is the working repository, record the commit and note that the collision-substrate family was already census-classified in R48.

---

# 4. Part 1 — H2 extraction

From the paper and any raw artifacts, extract and record:

- the reported regime/stage sequence, verbatim labels, in reported order (e.g., composite-only regime → two-frontier emergence → availability growth → mature substrate);
- the definitions: what a "family" is; what a "channel" is; what "two-frontier" and "composite-only" mean operationally; the candidate filter band(s); "strain"; "channel availability"; "mature anchor"; the substrate and its state;
- for each reported pattern: whether it is an ordering, a monotonicity, a freeze/saturation, a capacity or family-complexity dependence, a cross-substrate variability, or a rate/age statement (the last class is recorded and then excluded);
- numeric ages and thresholds, recorded as historical values only;
- whether the substrate has any destruction/removal mechanism.

Record definitions precisely enough to be reused for H3–H5 provenance disclosure.

---

# 5. Part 2 — Mapping by definition

For each H2 observable/notion, decide whether its definition, applied to 𝔷⁺, coincides *as a function* with a frozen R56 observable, a P1–P6 quantity, or a frozen R52/R53 observable. Record:

```text
H2 notion | definition | derived counterpart (frozen id) or UNMAPPED_{COMPUTABLE|INAPPLICABLE} | reason
```

Specifically adjudicate, by definition and not by name: whether "two-frontier" corresponds to a configuration requiring a specific number of co-served tokens or a specific parent-sharing pattern (P4 relevance); whether "family" corresponds to a grade, chain-multiplicity, or configuration class (P3/P4/P6 relevance); whether "strain" is a ledger functional (backlog/capacity, drain length) or a foam-family quantity; whether "channel availability" counts realized objects, pairs of objects, or something inapplicable.

---

# 6. Part 3 — Adjudication (sealed rule, operationalized)

- **Ordering test.** For each reported emergence ordering of families/stages, check consistency with P4 (configuration ordering, THEOREM at Γ ≤ 3) and P6 (cost ordering; THEOREM for the cost law, READOUT for the ordering). Give the explicit consistency map or the explicit contradiction.
- **Monotonicity test.** Each reported availability monotonicity against P1 (THEOREM) and the frozen observables' classes.
- **Freeze test.** Any reported saturation/locking of a family against P3 (THEOREM for the bound; READOUT for the order).
- **Capacity/complexity test.** Any reported dependence on substrate capacity or family complexity against P2/P4/P6.
- **Cross-substrate test.** If multiple independent substrates at equal age are reported, against P5 (THEOREM); else N/A.
- **Excluded.** Every rate/age statement, listed and marked excluded.

Verdict PASS / PARTIAL / FAIL per the sealed rule, with every mismatch and every UNMAPPED item at equal prominence. Mandatory model-family caveat section. If FAIL on P1, the pre-committed diagnosis path is invoked in the report (destruction mechanism yes/no) and nothing else.

---

# 7. Part 4 — Quarantined post-opening readout (optional, not adjudication)

For each `UNMAPPED_COMPUTABLE` H2 notion, compute its definition on the existing seeded derived trajectories (Γ ≤ 3 exact-typed; Γ ≥ 4 flagged) and report its direction. Label `POST_OPENING_READOUT_NOT_ADJUDICATION`. Only permitted future use: candidate target-blind freezing for H3–H5.

---

# 8. Hostile controls (8)

1. anything in the sealed preregistration altered; any derived-side item added after Commit A;
2. any round number aligned with a derived step; any rate compared;
3. any mapping by name, word, or count;
4. any content of H3–H5 read; H1 consulted;
5. Part 4 content in the verdict;
6. any modification of TG1, the cost law, the filtration, the alphabet scope, or frozen roots;
7. BELL2 opened;
8. any hand-produced hash; any placeholder.

---

# 9. Outputs (7 + stamp)

```text
OD0_R57_REPORT.md
OD0_R57_RESULTS.json
OD0_R57_COUNTEREXAMPLES.md                    (append-only; every mismatch and obstruction)
R57_INPUT_LOCK.json                           (protocol verbatim + hash; PDF hash verification; scope statement; repo pin)
R57_H2_EXTRACTION.json                        (Parts 0–1)
R57_MAP_TABLE_AND_ADJUDICATION.json           (Parts 2–3; Part 4 in a separate quarantined key)
R57_OUTPUT_MANIFEST.json
R57_PROVENANCE_STAMP.json                     (commit C)
```

Deterministic rerun: byte-identical JSON.

---

# 10. Verdict tree

Always, if the preregistration was verified unchanged and H2's hash verified before reading:

```text
OD0_R57_PASS_H2_OPENED_UNDER_SEALED_PROTOCOL
```

Primary: `H2_COMPARISON = {PASS | PARTIAL(list) | FAIL(contradicted prediction, pattern)}`.

Secondary: `MAP_TABLE = {mapped: n, unmapped_computable: n, unmapped_inapplicable: n}`; `RAW_BACKING = {claims with raw artifacts / paper-only}`; `H2_ENGINE_LAW = {verbatim}`; `DESTRUCTION_MECHANISM = {yes | no}`; `SENTINELS_H3_H5 = false`.

## R58 rule

- PASS or PARTIAL → R58 derives the **m-sibling alphabet** from the incidence structure (the m-descendant equality composite, its record algebra, A11R histories, and A12 counts, extending BELL0/R19 from two factors to m), closing the R56 scoping gap; R59 then takes the random-DAG cost theorem. H2 is spent after R57.
- FAIL on P1 → R58 first adjudicates the destruction question: whether the H2 substrate's removal mechanism is a foam-family artifact (no exact arrow into the constructor; R48 NO_MAP) or evidence of a missing constructor primitive. No premise is modified without the failure and its diagnosis on record; any modification is a new preregistered premise round.
- FAIL on P4 or P6 → R58 first re-examines the definition mapping that produced the contradiction, then the alphabet scope, before anything else.

---

# 11. Compact terminal return

```text
OD0-R57 OVERALL VERDICT:
COMMITS (A / B / C-stamp):
R56 STAMP PIN / PREREG HASH MATCH / PDF HASH MATCH / WORKTREE / BELL2 / H3–H5 SENTINELS / HAND HASHES:
SCOPE STATEMENT RECORDED AT COMMIT A (Γ ≤ 3 exact; Γ ≥ 4 flagged):
REPO PIN (commit; tree hash; raw artifacts inventoried):
H2 ENGINE OPPORTUNITY LAW (verbatim) / SUBSTRATE / DESTRUCTION MECHANISM:
H2 STAGE SEQUENCE (verbatim labels, order):
H2 NOTIONS EXTRACTED WITH DEFINITIONS (family, channel, two-frontier, composite-only, filter band, strain, availability, mature anchor):
MAP TABLE (mapped / unmapped_computable / unmapped_inapplicable):
ORDERING / MONOTONICITY / FREEZE / CAPACITY-COMPLEXITY / CROSS-SUBSTRATE TESTS:
EXCLUDED RATE/AGE STATEMENTS (listed):
H2 COMPARISON VERDICT: PASS / PARTIAL / FAIL:
MODEL-FAMILY CAVEAT:
POST-OPENING READOUT (quarantined; present yes/no):
HOSTILE CONTROLS: N/8:
DETERMINISTIC RERUN:
OUTPUT MANIFEST SHA-256 (in stamp):
RECOMMENDED SINGLE R58 MOVE:
```

---

# Adjudicator's registered prediction (Claude, pre-run)

PARTIAL. The composite-only → two-frontier ordering maps by definition to a configuration ordering and is consistent with P4; the family-dependence of early behavior is consistent with P6 at readout grade; "strain" is `UNMAPPED_INAPPLICABLE` (a foam-family quantity) or `UNMAPPED_COMPUTABLE` only if defined as a load ratio; "channel availability" is a pair count, mapped to a function of realized objects but its reported growth is excluded as a rate; no reported pattern contradicts P1 (no destruction mechanism in the substrate, or destruction present but availability of families still reported nondecreasing); cross-substrate variability N/A (single mature anchor). The H2 engine's opportunity law is neither T_sat nor TG1. This prediction constrains nothing in the run.
